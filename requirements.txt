# Flask and extensions
Flask==3.1.1
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.7.3
Werkzeug==3.1.3

# Database
SQLAlchemy==2.0.36

# Password hashing (comes with Flask/Werkzeug but explicit for clarity)
# werkzeug.security provides generate_password_hash and check_password_hash

# CORS (optional, currently commented out in app but available)
Flask-Cors==5.0.1

# Development and testing (optional)
python-dotenv==1.0.1
